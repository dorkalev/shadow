# build artifacts
website/app/target/
actions/shadow-ci/target/

# local instrument state (the DB is a projection of the md corpus + verify runs;
# adopting repos keep their own, generated by seed + agent/03)
*.db

# local evidence staging (posted to tickets, never committed to the code repo)
.shadow-evidence/
.playwright-mcp/
nohup.out

# terraform (lock file IS committed; state never is)
provision/**/.terraform/
provision/**/*.tfstate*
provision/**/tfplan
